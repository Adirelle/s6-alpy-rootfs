#!/bin/sh

cat >&2 <<EOF
Usage:
	$0 [-v verbosity] [-l live] [-c compiled] [-s source] command [args]

Options:

-v verbosity
	Set the verbosity of inner s6 related commands. Default: 0.
-l live
	Define where the live directory is. Default: /run/s6-rc
-c compiled
	Define where the compiled database symlink is. Default: /etc/s6-rc/compiled.
-s source
	Defien where the source directory is. Default: /etc/s6-rc/source.

Commands:

start names...
	Alias to s6-rc -u change names...
	
stop names...
	Alias to s6-rc -d change names...

restart names...
	Stop all named services then start them all again.
	
status [-d] [names...]
	Display the status of the given services/bundles.
	Without a name, select all services.
	With -d, show all their dependency tree.
	Show the atomics of bundles.
	Call s6-svstat on service directoy of longruns.
	Check the status of oneshots using s6-rc -a list.

poweroff
	Alias to s6-poweoff.

reboot
	Alias to s6-reboot.

update
	Recompile the source and update the running database.

mkbundle name [contents...]
	Create a new bundle with the given contents.

mklongrun [-d dep1,dep2,...] name
	Create a new longrun with the given dependencies and empty up and down scripts.

mkoneshot [-d dep1,dep2,...] name
	Create a new oneshot with the given dependencies and empty up and down scripts.

mklogsvc [-d dep1,dep2,...] name
	Create a couple of longruns:
	1. The first is named "name-svc" and depends on deps1,deps2, etc...
	2. The second is named "name-log", depends on "localfs" and call a logger scripts.
	Both are linked with a pipeline named "name".

help
	Prints this help.
EOF

exit 2
