#!/bin/execlineb -P
fdmove -c 2 1
s6-envdir env
multisubstitute {
	import HOSTNAME
	import -i interface
}
if { ifconfig $interface up }
udhcpc -f -R -i $interface -f $HOSTNAME -s /etc/s6-rc/source/iface-eth0/data/handler
