#!/bin/execlineb -S1

cd data
ifelse { test -x ./on-${1} } {
	foreground { ./on-${1} }
	import -u ?
	foreground { echo "${1} result: ${?}" }
	foreground { ip addr show }
	ip route show
}
foreground { echo "Unhandled event ${1}" }
exit 111
