#!/bin/execlineb -P
fdmove -c 2 1

getcwd CWD
import -u CWD
export ROOT ${CWD}/data

s6-envdir env
foreground {
	import -u IP
	/etc/s6-rc/scripts/resolvconf/set-fragment 10-dnscache "nameserver ${IP}"
}

redirfd -r 0 data/seed
s6-envuidgid dnscache

dnscache
