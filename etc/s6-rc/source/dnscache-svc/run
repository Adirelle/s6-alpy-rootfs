#!/bin/execlineb -P
fdmove -c 2 1

s6-envdir /etc/dnscache/env
foreground {
	import -u IP
	/etc/s6-rc/scripts/resolvconf/set-fragment 10-dnscache "nameserver ${IP}"
}

redirfd -r 0 /etc/dnscache/seed
s6-envuidgid dnscache

dnscache
